--- original/UndirectedVertex.h	2013-08-11 14:50:13.000000000 -0400
+++ UndirectedVertex.h	2016-04-04 09:04:38.173903354 -0400
@@ -122,17 +122,17 @@
 
 template<typename V, typename E>
 void UndirectedVertex<V,E>::successors(std::vector<V*>& out) {
-	getItems(adjMap, out);
+	Vertex<V,E>::getItems(adjMap, out);
 }
 
 template<typename V, typename E>
 void UndirectedVertex<V,E>::predecessors(std::vector<V*>& out) {
-	getItems(adjMap, out);
+	Vertex<V,E>::getItems(adjMap, out);
 }
 
 template<typename V, typename E>
 void UndirectedVertex<V,E>::adjacent(std::vector<V*>& out) {
-	getItems(adjMap, out);
+	Vertex<V,E>::getItems(adjMap, out);
 }
 
 template<typename V, typename E>
